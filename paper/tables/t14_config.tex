% generated by research/scripts/make_tables.py from configs/base.yaml, reports/results/benchmark*.json, training_summary_encoder_full_long.json; do not edit
\begin{table}[tbp]\centering\small
\caption{System configuration, read from configs/base.yaml and the locked block of each benchmark result file}\label{tab:t14_config}
\begin{tabular}{lp{0.74\linewidth}}
\toprule
component & setting \\
\midrule
input & Da-TACOS HPCP, 12 pitch classes; encoder view area-pooled to 256 frames \\
encoder & TCN, 6 residual blocks, kernel 3, dilations 1-32, 128 channels, receptive field 253 frames \\
pooling and head & mean and max over time, MLP to 128-d, L2-normalised; 645,504 parameters \\
training & supervised contrastive (temperature 0.1), 32 works x 2 recordings per batch, AdamW lr 0.001, cosine schedule, crop 0.6-1.0, random pitch rotation, seed 20260817 \\
Stage 1 & exact cosine search over all candidates, self excluded, top K = 30 \\
key handling & one of 12 rotations, chosen by cosine of time-averaged chroma profiles \\
alignment & cosine cost, subsequence DTW, steps [[1, 1], [2, 1], [1, 2]], weights [1.0, 2.0, 1.0], cost / path weight \\
fusion & alpha z(global) + (1 - alpha) z(alignment) within the shortlist \\
hub correction & alignment score minus lambda x mean score against 200 training-split probe queries \\
Run 1 locked & checkpoint sweep\_base, alignment frames 96, alpha 0.05, lambda - \\
Run 2 locked & checkpoint encoder\_full\_train, alignment frames 96, alpha 0.15, lambda 0.5 \\
Run 3 locked & checkpoint encoder\_full\_train, alignment frames 384, alpha 0.15, lambda 0.6 \\
Run 4 locked & checkpoint encoder\_full\_long, alignment frames 384, alpha 0.1, lambda 0.6 \\
\bottomrule
\end{tabular}
\end{table}
